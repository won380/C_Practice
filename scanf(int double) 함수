#include <stdio.h>

// #define _CRT_SECURE_NO_WARNINGS

int main(void)
{
	int age;
	double height;

	printf("나이와 키를 입력하세요:");
	scanf_s("%d %lf", &age, &height);
	printf("나이는 %d살, 키는 %.1lf입니다.\n",age, height);

	return 0;

}
